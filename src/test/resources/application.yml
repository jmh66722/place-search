app:
  version: test.1.0.0
  name: place-search-test

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        use_sql_comments: true
        format_sql: true

logging:
  level:
    root: info
    org.example.advice.*: debug
    org.example.service.*: debug